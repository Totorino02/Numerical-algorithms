"""self.dim = int(input("Entrez la valeur de la dimenssion: "))
            self.matrix = list()
            # ajout d'un tableau de n= dim valeurs provenant de l'entrée
            for i in range(self.dim):
                self.matrix.append([(float)(j) for k,j in enumerate((input("Ligne {} -> ".format(i+1))).split()) if k < self.dim])
            #lecture et sauvegarde de B
            self.vect = [(int)(x) for x in (input("vecteur Bn: ")).split()]"""

"""for i in range(self.dim):
    if len(self.matrix[i]) != self.dim:
        raise IndexError("Votre matrix n'est pas carré !")
for i in range(self.dim):
    self.matrix[i].append(self.vect[i])"""